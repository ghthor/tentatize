// tentatize is an cli application to manage
// a tentative schedule/planner.
// The storage format will be human readable
// and writable and should be convient to edit
// directly using their normal text editor(ex: vim).
// The data is stored as text files and the
// changes over time will be monitored and
// recorded in a git repository.
package main

func main() {
}
